<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog!</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="css/bootstrap.min.css" />
</head>

<body>
    <header>
        <nav class="navbar bg-body-tertiary">
            <div class="container-fluid">
                <a class="navbar-brand" href="/home">BLOG!</a>
                <button class="btn btn-primary" id="login-navbar">Login</button>
                {{#if logged_in}}
                <button class="btn btn-primary" id="new-post">New Post</button>
                <button class="no-button btn btn-danger" id="logout">Logout</button>
                {{/if}}
            </div>
        </nav>
    </header>
    <main class="container container-fluid mt-5">
        <!-- Render the sub layout -->
        {{{ body }}}
    </main>
    <footer>

    </footer>


    <script src="/js/login.js"></script>
    <!-- Render script for logged in users only -->
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    <script src="/js/newPost.js"></script>
    {{/if}}
</body>

</html>