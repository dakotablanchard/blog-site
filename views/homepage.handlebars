<!-- Loop over each object in the projects array -->
{{#each posts as |post| }}
<div class="row mb-4 post">
  <div class="col-md-5">
    <h2>{{post.date_created}}</h2>
    <p>{{post.content}}</p>
    <p>Created by: {{post.user.name}}</p>
  </div>
</div>
{{/each}}